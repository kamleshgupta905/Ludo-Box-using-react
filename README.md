# Ludo Feel

Mobile-first Ludo game interface built with React, TypeScript, TanStack Start, and Tailwind CSS.

## Development

```sh
npm install
npm run dev
```

## Cloud APK build

Push the project to GitHub and run the **Build Android APK** workflow from the Actions tab. The APK is available as the `ludo-feel-debug-apk` workflow artifact.
